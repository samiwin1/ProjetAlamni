<form method="post" action="{{ path('app_reponsereclamation_delete', {'id': reponsereclamation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponsereclamation.id) }}">
    <button class="btn">Delete</button>
</form>
